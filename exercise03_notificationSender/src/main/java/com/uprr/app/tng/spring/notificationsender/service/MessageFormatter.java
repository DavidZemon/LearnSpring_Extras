package com.uprr.app.tng.spring.notificationsender.service;

public class MessageFormatter {
    public String formatMessage(final String message) {
        // Imagine some complex code here used to format an email message
        return message;
    }
}
